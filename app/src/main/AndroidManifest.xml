<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- SMS permissions (RECEIVE_SMS / READ_SMS) live in src/full/AndroidManifest.xml so the
         Play flavor ships without them. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- So the "Report an issue" mail intent can resolve an email app under package visibility. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:name=".XpendiqApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Xpendiq">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- SmsBroadcastReceiver is declared in src/full/AndroidManifest.xml (full flavor only). -->

        <service
            android:name=".sms.MessageNotificationListener"
            android:label="@string/notif_listener_label"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- TestIngestReceiver is registered in src/debug/AndroidManifest.xml only -->
    </application>

</manifest>
